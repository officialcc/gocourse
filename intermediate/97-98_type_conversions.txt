package main

import "fmt"

func main() {

	var a int = 32
	b := int32(a)
	c := float64(b) // float64 = target type; b = source type
	// d := bool(true)
	e := 3.14
	f := int(e)
	fmt.Println(f, c) // prints 3 32 <- 3.14 loses its decimal points

	// Type(value)

	g := "Hello @"
	var h []byte
	h = []byte(g)
	fmt.Println(h)
	i := []byte{255, 120, 72}
	j := string(i)
	fmt.Println(j)
}
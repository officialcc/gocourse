package main

import (
	"encoding/json"
	"fmt"
	"log"
)

type Person struct {
	// FirstName string `json:"first_name"`
	FirstName string `json:"first_name" db:"firstn" xml:"first"` // declaring multiple tags for JSON, for database column, and for xml
	LastName  string `json:"last_name,omitempty"`
	// Age       int    `json:"age,omitempty"`
	Age       int    `json:"-"`
}

func main() {

	person := Person{FirstName: "Buster", LastName: "Douglas", Age: 48}
	// person := Person{FirstName: "Buster", LastName: "", Age: 48}
	// person := Person{FirstName: "Buster", Age: 48}
	// person := Person{FirstName: "Buster", LastName: "", Age: 0}
	jsonData, err := json.Marshal(person)
	if err != nil {
		log.Fatalln("Error marshalling struct:", err)
	}
	fmt.Println(string(jsonData))
}